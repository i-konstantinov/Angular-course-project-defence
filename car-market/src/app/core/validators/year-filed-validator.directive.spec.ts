import { YearFiledValidatorDirective } from './year-filed-validator.directive';

describe('YearFiledValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new YearFiledValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
