import { PriceFieldValidatorDirective } from './price-field-validator.directive';

describe('PriceFieldValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceFieldValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
