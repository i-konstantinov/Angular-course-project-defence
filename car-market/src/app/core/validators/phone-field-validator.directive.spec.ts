import { PhoneFieldValidatorDirective } from './phone-field-validator.directive';

describe('PhoneFieldValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneFieldValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
