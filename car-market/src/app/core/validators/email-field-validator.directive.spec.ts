import { EmailFieldValidatorDirective } from './email-field-validator.directive';

describe('EmailFieldValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailFieldValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
